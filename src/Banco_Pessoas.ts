import { Pessoa } from "./Pessoa";

export class Banco_Pessoas {
    private pessoas: Pessoa[] = [];
    private hashmap: Map<string, string> = new Map(); // Placeholder for the database connection
    
}